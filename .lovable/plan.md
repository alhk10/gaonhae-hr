In `src/pages/public/PublicGradingPayment.tsx`, wrap the ProofOfPaymentUpload (lines 685-690) and Submit button (lines 692-698) in a conditional that only renders them when `selectedSlotId` is set. The payment method/info block (already gated by `selectedItems.length > 0`) stays as-is.